<ul class="sidebar-menu">
	#foreach ($nav_item in $nav_items)
		#if($nav_item.getLayout().getExpandoBridge().getAttribute($sideNavigation) == true)
			## we are only interested in second level menu
			#if ($nav_item.hasChildren())
				#foreach ($nav_child in $nav_item.getChildren())
				#if ($is_signed_in || ($nav_child.getLayout().getExpandoBridge().getAttribute($hideGuest) == false))
				
					#if ($nav_child.isSelected())
						#set( $selected = " active " )
					#else
						#set( $selected = "" )
					#end
					
					<li class="$selected$childClass">
					
						
							<a href="$nav_child.getURL()">
								<span>$nav_child.icon() $nav_child.getName()</span> 
							</a>
							
						#end
						
					</li>
				#end
			#end
		#end
	#end

</ul>